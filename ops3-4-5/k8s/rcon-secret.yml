apiVersion: v1
kind: Secret
metadata:
  name: minecraft-rcon-secret
  namespace: default
type: Opaque
stringData:
  # RCON password used by both the Minecraft server and minecraft-exporter.
  # Change this value before deploying. Do not commit real credentials to git
  # — consider using a sealed-secret or external-secrets operator in production.
  RCON_PASSWORD: "changeme123"
